// slider from skick
$(document).ready(function () {
    $(".compliment__wrapper").slick({
        speed: 1200,
        // controls: true,
        // dots: true,
        arrows: false,
        pauseOnHover: true,
        autoplay: true
    });
});